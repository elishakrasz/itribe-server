scalar Upload

input CreateCommunityInput {
  name: String!
  picture: Upload
  category: String!
  country: String!
  latitude: Float!
  longitude: Float!
}

type Mutation {
  createCommunity(input: CreateCommunityInput!): Boolean!
}
